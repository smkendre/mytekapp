import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mytekinnovations.app',
  appName: 'mytek-mobile-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
